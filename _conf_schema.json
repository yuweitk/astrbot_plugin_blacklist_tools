{
  "max_blacklist_duration": {
    "type": "int",
    "default": 86400,
    "minimum": 1,
    "description": "黑名单最长时长",
    "hint": "仅对LLM工具生效，管理员命令不受限制"
  },
  "allow_permanent_blacklist": {
    "type": "bool",
    "default": true,
    "description": "允许永久黑名单",
    "hint": "仅对LLM工具生效，管理员命令不受限制"
  },
  "allow_blacklist_admin": {
    "type": "bool",
    "default": false,
    "description": "允许拉黑管理员",
    "hint": "开启时时也会阻止管理员的消息"
  },
  "show_blacklist_status": {
    "type": "bool",
    "default": false,
    "description": "向被拉黑用户显示拉黑状态"
  },
  "blacklist_message": {
    "type": "string",
    "default": "[连接已中断]",
    "description": "黑名单提示消息",
    "hint": "当show_blacklist_status为true时显示给被拉黑用户"
  },
  "auto_delete_expired_after": {
    "type": "int",
    "default": 86400,
    "description": "自动删除过期多久的黑名单（秒），设置为-1为不删除",
    "minimum": -1
  }
}